import 'package:flutter/material.dart';

class CurationStore extends StatelessWidget {
  const CurationStore({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
